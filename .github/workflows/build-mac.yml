name: Build Mac App

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build (e.g. v1.0.3)'
        required: true
        default: 'v1.0.3'

permissions:
  contents: write

jobs:
  build-mac:
    runs-on: macos-latest
    
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref }}
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      
      - name: Install system dependencies
        run: |
          brew install qt@5
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      
      - name: Build Mac app
        run: |
          pyinstaller jd-supplier-label-generator-mac.spec
      
      - name: Create DMG
        env:
          TAG_NAME: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}
        run: |
          cd dist
          VERSION=${TAG_NAME#v}
          DMG_NAME="jd-supplier-label-generator-${VERSION}.dmg"
          
          # 创建临时目录用于 DMG 内容
          mkdir -p dmg_temp
          cp -r jd-supplier-label-generator.app dmg_temp/
          ln -s /Applications dmg_temp/Applications
          
          # 创建 DMG
          hdiutil create -volname "JD Supplier Label Generator" -srcfolder dmg_temp -ov -format UDZO "$DMG_NAME"
          
          # 清理临时目录
          rm -rf dmg_temp
          
          echo "DMG_NAME=$DMG_NAME" >> $GITHUB_ENV
      
      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}
          files: dist/${{ env.DMG_NAME }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
